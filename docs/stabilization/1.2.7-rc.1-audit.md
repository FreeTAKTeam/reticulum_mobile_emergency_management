# REM 1.2.7-rc.1 Stabilization Audit

This ledger is the authoritative record for the `1.2.7-rc.1` stabilization
work. A subsystem is complete only when its code review, automated evidence,
and relevant release evidence are recorded here. Critical, high, and medium
correctness findings cannot be deferred.

## Baseline

- Stabilization branch: `agent/rem-1-2-7-rc1-stabilization`
- Base `main` commit: `55a3560e36eb0db9108d7b51bc27b3ef1aef998c`
- Pinned LXMF-rs release: `v0.9.5`
- Pinned LXMF-rs commit: `7cafc5b4be21ff4f777d0f2300cfb79e5d0da23c`
- Rust toolchain gate: `1.88`, edition `2021`
- Android is the supported release target. iOS compilation is out of scope.

## Subsystem ledger

| Subsystem | Review focus | Status | Evidence or remaining gate |
| --- | --- | --- | --- |
| Runtime lifecycle and readiness | start, stop, restart, unavailable interfaces, cancellation | In progress | Rust lifecycle and full browser readiness suites pass; physical-device restart/reconnect gate pending |
| Storage and persistence | SQLite errors, transactions, timestamp and counter bounds | Complete | causal error capture, checked conversions, strict clippy, and Rust tests pass |
| Messaging and delivery | channels, timeouts, terminal delivery states, SDK send failures | In progress | strict clippy and Rust tests pass; device delivery matrix pending |
| Mission replication | EAM, MECP, checklist merge, duplicate suppression | In progress | Rust and all 74 Playwright cases pass; device matrix pending |
| Telemetry and SOS | coordinate bounds, activation/update/cancel/recovery | In progress | checked conversions and Rust tests pass; device matrix pending |
| Plugin host and examples | Binder lifecycle, permissions, cleanup, sample plugins | Complete | cleanup exceptions are logged; SDK/example JVM builds and two API 35 Binder instrumentation cases pass |
| JNI and UniFFI | input parsing, last-error contract, panic containment | Complete | all 85 Java exports contained; structured error and panic tests pass |
| Node client | stable rejected-promise classification and builds | Complete | clean `npm ci`, 15 unit tests, and TypeScript build pass |
| Vue stores and views | startup overlay, disabled RCH, unavailable network | Complete | 88 unit tests, typecheck, web/mobile builds, and all 74 desktop/mobile Playwright cases pass |
| Android services | service lifecycle, RNode BLE/USB, notification state | In progress | JVM tests, API 35 Binder instrumentation, all three native ABIs, and debug packaging pass; physical BLE/USB matrix pending |
| CI and dependency compatibility | pinned/current LXMF, strict checks, complete E2E | In progress | workflow repaired locally; hosted checks pending |
| Packaging and release | signed APK/AAB, checksums, install/runtime identity | Pending | exact merged-commit build and remote prerelease pending |
| Documentation | product, developer, architecture, examples, recovery | Complete | authoritative Markdown set, examples, recovery guidance, release notes, and archive labels updated |

## Findings

| ID | Severity | Finding | Resolution |
| --- | --- | --- | --- |
| REM-127-001 | High | RCH registration bootstrap blocked local runtime startup and left the splash overlay visible when RCH or an interface was unavailable. | Local startup now completes independently; RCH refresh runs asynchronously and logs failure. Four desktop/mobile regressions pass. |
| REM-127-002 | High | JNI exports relied on the non-unwinding ABI instead of returning a compatible failure when Rust panicked. | A reusable boundary attribute contains panics on all 85 Java exports, sets `InternalError`, and returns `1` or null. |
| REM-127-003 | High | 393 Rust error conversions discarded the original I/O, database, serialization, channel, network, or SDK error. | All conversions now retain and log causal context through a stable internal failure record; JNI consumes that context without changing `NodeError`. |
| REM-127-004 | High | npm and Rust dependency graphs contained known advisories. | Compatible lockfile updates remove active npm advisories and the RustSec `anyhow` vulnerability. Clean `npm ci`, npm audit, and cargo audit gates pass. |
| REM-127-005 | Medium | Numeric casts crossed timestamps, SQLite values, counters, coordinates, sizes, and timeout boundaries without an explicit policy. | Checked/saturating helpers and exact integer-to-float conversion replace risky casts; boundary tests pass. |
| REM-127-006 | Medium | Ambiguous boolean/argument-heavy Rust calls and ignored value-returning APIs made misuse easier. | Typed request structures and `#[must_use]` annotations added at defect-prone boundaries. |
| REM-127-007 | Medium | Vite watched generated Android/Rust/test trees, contributing to watcher exhaustion. | Generated and temporary trees are ignored by the development watcher. |
| REM-127-008 | Medium | CI ran only Playwright smoke coverage and the current-LXMF job incorrectly enforced the pinned lockfile. | CI now runs the complete Playwright matrix and creates an ephemeral current-LXMF lockfile while pinned `v0.9.5` remains locked. |
| REM-127-009 | Medium | Sample plugins silently swallowed cleanup and callback failures. | Cleanup remains non-fatal but is explicitly logged; callback failures are warnings. |

## Accepted low-risk debt

| Item | Reason | Follow-up |
| --- | --- | --- |
| `bincode 1.3.3` is unmaintained (RUSTSEC-2025-0141). | No vulnerability is reported; replacement changes persisted/wire serialization and is too risky for an RC hardening slice. | Migrate with compatibility fixtures in the next minor development cycle. |
| `paste 1.0.15` is unmaintained (RUSTSEC-2024-0436). | It is transitive build-time debt with no vulnerability and no compatible lock-only removal. | Remove when the owning dependency publishes a compatible release. |
| Existing PDF and DOCX manuals remain in the tree. | They are retained only as historical snapshots for users who already reference them. | Markdown is authoritative; archive labeling is part of this release documentation. |

## Automated evidence

- `cargo +1.88 fmt --check`: pass.
- `cargo +1.88 fetch --locked` against LXMF-rs `v0.9.5`: pass.
- `cargo +1.88 clippy --all-targets -- -D warnings`: pass.
- `cargo +1.88 test --locked`: 362 passed.
- `cargo audit`: no vulnerabilities; two accepted unmaintained warnings listed above.
- Clean `npm ci`: pass; `npm audit --audit-level=moderate`: zero vulnerabilities.
- Source-size gate: 462 first-party source files checked with no exceptions.
- Application unit tests: 88 passed; node-client unit tests: 15 passed.
- Node-client TypeScript build and Vue typecheck: pass.
- Web and mobile production builds: pass.
- Playwright desktop/mobile Chromium matrix: all 74 cases pass, including the four startup/readiness regressions.
- Capacitor Android sync: pass.
- Android JVM tests, SDK/example plugin builds, and debug packaging: pass.
- Android API 35 Binder instrumentation: two integration cases pass.
- Android native release libraries: `arm64-v8a`, `armeabi-v7a`, and `x86_64` built from pinned LXMF-rs `v0.9.5`; ELF alignment check passes.
- Debug APK contains `libreticulum_mobile.so` for all three required ABIs.

The remaining hosted-CI, physical-device, exact-merge signing, and remote release
evidence must be appended before this ledger can be marked complete.

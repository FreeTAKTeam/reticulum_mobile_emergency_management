package network.reticulum.emergency.wearables;

public enum WearableConnectionState {
    DISCOVERED,
    CONNECTING,
    CONNECTED,
    SERVICE_DISCOVERED,
    HEART_RATE_AVAILABLE,
    SUBSCRIBED,
    DISCONNECTED,
    ERROR,
    UNSUPPORTED
}
